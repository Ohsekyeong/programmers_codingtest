function solution(phone_number) {
    
    let i= String(phone_number) //입력 값 문자열로 교체
    
   let a = i.substr(-4) //문자열에서 뒷 4자리 자르기
   
   let b = i.slice(0,-4) //문자열에서 뒷 4자리 제외하고 앞에 자르기
 
   let c=  b.replace(/[0-9]/gi, '*');
   
   
   return c+a
   
   
   //String() = 문자열 교체를 의미합니다. 앞에 s가 아닌 S를 붙여야 실행되요.
   
   // substr(시작위치, 길이) = 시작위치부터 길이만큼 문자열을 자르는 함수. (길이)를 생략하면 시작부터 끝까지 자릅니다. 
   // 예를 들어, A= '자바스크립트' 문자열이 있으면 A.substr(0,2)='자바' , A.substr(2)= '스크립트'
   // 저는 -4를 시작위치로 잡았는데 이렇게 되면 "자바/ 스크립트" 처럼 뒤에서 4번째부터 자릅니다. 
   
  // slice(시작위치, 종료위치) = 뒤에서부터 자르기 편한 함수입니다. substr와 다른 점은 '길이'만큼 자르는 게 아니고 '종료 위치'까지 자르는 점이에요.
  // 예를 들어, A= '자바스크립트' A.slice(0,2)는 0번째부터 2번째 전인 1번째까지 자르는 거에요. '자바'로 나옵니다. 
  // 문제 풀고나서 더 찾아보니 i.slice(-4)를 했어도 뒷 4자리를 자를 수 있더라고요. 
  
  //함수 내에 5 줄이나 쓰긴 했지만 제 머리로는 자르고 붙이는 게 가장 편한 방식이었습니다! 
  
